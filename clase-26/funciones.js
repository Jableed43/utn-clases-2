//funcion nombrada
function suma(a, b) {
  return a + b;
}

suma(2, 4);

//funcion anonima
const resta = function (a, b) {
  return a - b;
};

resta(10, 2);

//funcion flecha 1
const multiplicacion = (a, b) => a * b;

multiplicacion(2, 2);

//funcion flecha 2

const division = (a, b) => {
  return a / b;
};

division(4, 2);
